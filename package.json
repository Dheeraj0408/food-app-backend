{
  "name": "food-app-backend",
  "version": "1.0.0",
  "description": "Backend server for a food app",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port 8081",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
